import { ShieldCheck, KeyRound, Eye, UserCheck } from 'lucide-react';
export function Security(){ return <section className="mx-auto max-w-6xl px-5"><div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><div className="industrial-panel rounded-[24px] p-8"><p className="technical text-[10px] font-bold text-accent">CONTROL PRINCIPLES</p><h2 className="mt-3 text-3xl font-extrabold">Conservative by design.</h2><p className="mt-4 max-w-2xl text-sm leading-6 text-slate">PAIR is decision support. It should expose uncertainty, preserve evidence context and make it clear where human verification is still required.</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{[['Evidence context',Eye],['Server-side credentials',KeyRound],['Officer authority',UserCheck],['Review traceability',ShieldCheck]].map(([t,I])=>{const Icon=I as any;return <div key={t as string} className="industrial-recessed rounded-xl p-4"><Icon size={18} className="text-accent"/><p className="mt-3 text-sm font-bold">{t as string}</p></div>})}</div></div><div className="screen rounded-[24px] p-7"><p className="technical text-[9px] text-white/45">VERIFICATION STATE</p><div className="mt-7 space-y-5">{['Source document','Evidence extraction','Compliance evaluation','Officer review'].map((x,i)=><div key={x} className="flex items-center gap-3"><span className={`h-3 w-3 rounded-full ${i<3?'bg-emerald-400':'bg-amber-400'}`}/><span className="text-sm text-white/75">{x}</span><span className="ml-auto technical text-[8px] text-white/40">{i<3?'READY':'REQUIRED'}</span></div>)}</div></div></div></section>}
