'use client';
import { motion } from 'framer-motion';
import { Cpu, FileSearch, Scale, Database, ShieldCheck } from 'lucide-react';
export function Orchestration(){
 const blocks=[['DOCUMENT PROCESSING','PDF text extraction',FileSearch],['AI ANALYSIS','Requirements + evidence',Cpu],['COMPLIANCE ENGINE','Requirement-level verdicts',Scale],['TRACEABILITY','Audit events',Database],['DECISION SUPPORT','Recommendation for officer',ShieldCheck]] as const;
 return <section id="verification" className="mx-auto max-w-6xl px-5"><div className="industrial-panel overflow-hidden rounded-[28px] bg-[#2d3436] p-6 text-white shadow-floating md:p-10"><div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="technical text-[10px] font-bold text-white/50">SYSTEM ARCHITECTURE</p><h2 className="mt-3 text-3xl font-extrabold tracking-[-.035em] md:text-4xl">One review surface. Multiple evidence stages.</h2></div><div className="technical flex items-center gap-2 text-[9px] text-white/55"><span className="h-2 w-2 rounded-full bg-emerald-400 shadow-[0_0_9px_rgba(34,197,94,.8)]"/> SERVER-SIDE AI</div></div><div className="mt-9 grid gap-3 md:grid-cols-5">{blocks.map(([t,d,Icon],i)=><motion.div key={t} whileHover={{y:-3}} className="rounded-xl border border-white/10 bg-white/[.055] p-4"><Icon size={20} className="text-white/80"/><p className="technical mt-5 text-[9px] font-bold text-white/55">{t}</p><p className="mt-2 text-sm text-white/80">{d}</p><div className="mt-5 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-[#ff4757]" style={{width:`${70+i*6}%`}}/></div></motion.div>)}</div><p className="mt-8 max-w-3xl text-sm leading-6 text-white/55">Government verification connectors are treated as modular integration points. Where an authorized external source is unavailable, PAIR keeps the state explicit instead of fabricating a verification result.</p></div></section>
}
