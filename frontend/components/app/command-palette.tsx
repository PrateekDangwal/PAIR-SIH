'use client';
import {useEffect,useState} from 'react';
import {AnimatePresence,motion} from 'framer-motion';
import {Search} from 'lucide-react';
import {useRouter} from 'next/navigation';
const commands=[['Overview','/app'],['Bid Reviews','/app/projects'],['AI Assistant','/app/chat'],['Documents','/app/artifacts'],['Risk & Audit','/app/activity'],['AI Models','/app/models'],['Settings','/app/settings']];
export function CommandPalette(){const [open,setOpen]=useState(false);const [q,setQ]=useState('');const router=useRouter();useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(v=>!v)}if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',f);return()=>window.removeEventListener('keydown',f)},[]);return <AnimatePresence>{open&&<div className="fixed inset-0 z-[80] bg-ink/30 p-4 pt-[15vh] backdrop-blur-sm" onClick={()=>setOpen(false)}><motion.div initial={{opacity:0,y:-12,scale:.98}} animate={{opacity:1,y:0,scale:1}} className="mx-auto max-w-lg industrial-panel rounded-2xl overflow-hidden" onClick={e=>e.stopPropagation()}><div className="flex items-center gap-3 border-b border-slate/15 p-4"><Search size={18} className="text-slate"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Jump to a PAIR workspace…" className="w-full bg-transparent text-sm outline-none placeholder:text-slate/60"/></div><div className="p-2">{commands.filter(c=>c[0].toLowerCase().includes(q.toLowerCase())).map(([l,h])=><button key={h} onClick={()=>{router.push(h);setOpen(false)}} className="w-full rounded-xl px-3 py-3 text-left text-sm font-bold hover:bg-white/50">{l}</button>)}</div></motion.div></div>}</AnimatePresence>}
