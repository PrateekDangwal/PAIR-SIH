import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Sign In - PAIR',
  description: 'Sign in to your PAIR account',
};

export default function LoginPage() {
  return <LoginForm />;
}
