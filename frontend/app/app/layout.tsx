'use client';
import {useEffect} from 'react';import {useRouter} from 'next/navigation';import {useAuth} from '@/hooks/use-auth';import {Sidebar} from '@/components/app/sidebar';import {Topbar} from '@/components/app/topbar';import {CommandPalette} from '@/components/app/command-palette';
export default function AppLayout({children}:{children:React.ReactNode}){const {user,isLoading}=useAuth();const router=useRouter();useEffect(()=>{if(!isLoading&&!user)router.replace('/login')},[isLoading,user,router]);if(isLoading||!user)return <div className="min-h-screen chassis"/>;return <div className="min-h-screen chassis"><div className="flex min-h-screen"><Sidebar/><div className="min-w-0 flex-1"><Topbar/><CommandPalette/><main className="relative min-h-[calc(100vh-1px)]">{children}</main></div></div></div>}
